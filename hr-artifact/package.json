{
  "name": "corcondev-hr-artifact",
  "private": true,
  "version": "1.0.0",
  "description": "Shim host for the existing CorConDev HR Claude artifact. Separate Netlify site — not the company portal.",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "build": "node scripts/prepare-google-sa.js",
    "prepare:google-sa": "node scripts/prepare-google-sa.js"
  },
  "dependencies": {
    "@netlify/blobs": "^11.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
